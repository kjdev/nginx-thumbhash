server {
  listen 80 default_server;
  listen [::]:80 default_server;

  root /var/lib/nginx/html;

  location ~ ^/thumbhash/(?<data>.+)$ {
    thumbhash_render $data base64=url;

    rewrite ^/thumbhash/(.+)$ /$1 break;
  }

  location ~ ".(jpe?g|png|gif)$" {
    thumbhash_filter;
  }
}
